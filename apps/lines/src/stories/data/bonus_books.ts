// Data lives in bonus_books.json — same treatment as base_books.ts (whose size
// OOMs Vite's TS transform); kept as JSON for consistency and headroom.
import books from './bonus_books.json';

export default books;
